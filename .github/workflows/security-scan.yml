name: Security Scan

on:
  schedule:
    - cron: '23 4 * * 1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security:
    name: Security
    uses: ./.github/workflows/_security-scan.yml
