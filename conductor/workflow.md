# Conductor Workflow
> Generated via /conductor-manage, /conductor-implement, & /concise-planning

All active tracks are stored in `conductor/tracks/`.
When a track is fully merged, its artifacts and plans must be immediately archived to `conductor/_archive/`.
